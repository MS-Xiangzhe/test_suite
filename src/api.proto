syntax = "proto3";

service ImageService {
   // (Method definitions not shown)
}

message ScreenShotRequest {
   string window_name = 1;
}

service GUICatcherService {
   rpc ScreenShot (string ) returns (ScreenShotReply) {}
}